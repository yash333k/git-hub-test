pipeline {
environment {
registry = "daidasunny/nginxsunil"
registryCredential = 'dockerhub_id'
}
  agent any

  stages {
    stage("Build image and container") {
      steps {
        sh 'docker build -f ./Dockerlab-nginx/Dockerfile -t nginxsunil:1.0 .'
        sh 'docker run -d -p 3244:80 nginxsunil:1.0'
      }
    }
    stage('Deploy Image') {
       steps {
           withDockerRegistry([credentialsId: "dockerhub_id", url: ""]) {
               sh 'docker push daidasunny/nginxsunil:sunil'
           }
       }
    }
  }
}
